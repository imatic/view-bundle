{#
    variables:
    - form
    - form_theme
#}

{% block form %}
    {% form_theme form form_theme|default('bootstrap_3_horizontal_layout.html.twig') %}

    {{ form_stylesheet(form) }}

    <div class="imatic-view-form">
        {{ form(form, {attr: {novalidate: 'novalidate' } }) }}
        {{ form_javascript(form) }}
    </div>
{% endblock %}
