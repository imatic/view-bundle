{% block layout %}
    <div class="row">
        {% set class = 'col-md-' ~ first.size|default(12) ~ (first.offset|default('') ? ' col-md-offset-' ~ first.offset : '') %}
        <div class="{{ class }}">
            {% block first %}
            {% endblock %}
        </div>
    </div>
{% endblock %}