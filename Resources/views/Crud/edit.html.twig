{% extends '::base.html.twig' %}

{% block headline (resource.config.name|capitalize ~ ' ' ~ action.name)|trans({('%'~resource.config.name~'%'): item|default('new')}, resource.config.translation_domain) %}

{% set page_actions = imatic_view_resource_create_page_actions(resource, action.name, item.id|default(null)) %}

{% block action %}

    {% include 'ImaticViewBundle:Component:form.html.twig' %}

{% endblock %}
