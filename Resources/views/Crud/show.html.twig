{% extends '::base.html.twig' %}

{% block headline (resource.config.name|capitalize ~ ' ' ~ action.name)|trans({('%'~resource.config.name~'%'): item}, resource.config.translation_domain) %}

{% set page_actions = imatic_view_resource_create_page_actions(resource, action.name, item.id) %}

{% block action %}
    {% set fields = action.fields %}

    {% set show = {
    translationDomain: resource.config.translation_domain
    } %}

    {% include 'ImaticViewBundle:Component:show.html.twig' %}

{% endblock %}
