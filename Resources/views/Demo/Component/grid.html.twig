{% extends 'ImaticViewBundle:Demo:base.html.twig' %}

{% block headline 'Grid component' %}

{% block action %}

    {#{% set table = {'selectable': false, 'selectableColumn': 'id', 'sortable':true} %}#}
    {% set columns = [
        {'name': 'name', 'format': 'text'},
        {'name': 'age', 'format': 'number'},
        {'name': 'active', 'format': 'boolean'},
        {'name': 'lastOnline', 'format': 'date'},
        {'name': 'phone', 'format': 'phone'},
        {'name': 'email', 'format': 'email'},
        {'name': 'url', 'format': 'url'}
    ] %}

    {{ include('ImaticViewBundle:Component:table.html.twig') }}
    {{ include('ImaticViewBundle:Component:grid.html.twig') }}

{% endblock %}
